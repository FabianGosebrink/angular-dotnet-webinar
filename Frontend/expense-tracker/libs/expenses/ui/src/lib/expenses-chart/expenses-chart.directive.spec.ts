import { ExpensesChartDirective } from './expenses-chart.directive';

describe('ExpensesChartDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpensesChartDirective();
    expect(directive).toBeTruthy();
  });
});
